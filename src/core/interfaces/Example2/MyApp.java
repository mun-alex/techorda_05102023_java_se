package core.interfaces.Example2;

public class MyApp {
    public static void encodePassword(Encoding encoding) {
        encoding.encode();
    }

    public static void decodePassword(Decoding decoding) {
        decoding.decode();
    }
}
